import React from 'react';
import { Box } from '@chakra-ui/react'

const NavWidths = {
  base: '0',
  lg: '30%',
};

const SideNav = () => (
  <Box height="100%" w={NavWidths}>
    Adrian
  </Box>
);

export default SideNav;
