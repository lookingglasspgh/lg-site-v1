import { colors } from './theme';

const gradients = {
  black: 'linear-gradient(137deg, rgba(2,3,20,1) 0%, rgba(27,28,44,1) 50%, rgba(53,53,67,1) 100%)',
  gray: 'linear-gradient(137deg,rgba(167, 165, 158, .9) 0%, rgba(239, 236, 226, 1) 100%)',
};

const commonWidths = {
  maxSectionWidth: '75rem',
};

const commonStyles = {
  commonWidths,
  colors,
  gradients,
};

export default commonStyles;
